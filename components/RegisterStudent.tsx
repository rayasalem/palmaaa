
import React from 'react';

// This component is deprecated. Student role is now merged with Broker.
// Retained temporarily to prevent import errors if lazily loaded, but logic is removed.

const RegisterStudent: React.FC<any> = () => {
  return null;
};

export default RegisterStudent;
